package Code;

/**
 * Contains keys used for retrieving current weather data.
 */
public enum CurrentWeatherKeys {
      currTemp, currTempFeel, humidity, maxTemp, minTemp, pressure, visibility, weatherDesc, weatherIcon, windDegree, windSpeed
}
